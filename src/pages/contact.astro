---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Contact | SGA Industrial Automations"
  description="Contact SGA Industrial Automations in Hyderabad for industrial automation, IIoT, control panels and engineering support."
>
  <section class="page-header">
    <h1>Contact</h1>
    <p>SGA Industrial Automations</p>
  </section>

  <section class="contact-grid">
    <!-- Left: Office Details (like SR head office block) -->
    <div class="contact-info">
      <h2>Our Head Office</h2>
      <p class="label">Address:</p>
      <p>
        Flat.no.101, R V Gayathri Arcade,<br />
        Gayatri Nagar, Godavari Homes,<br />
        Quthbullapur, Hyderabad,<br />
        Telangana, India – 500055
      </p>

      <p class="label">Phone:</p>
      <p class="phone">
        📞 <a href="tel:+917673902729">+91 76739 02729</a>
      </p>

      <p class="call-note"><strong>Call us for more details</strong></p>

      <a class="call-btn" href="tel:+917673902729">Call Now</a>
    </div>

    <!-- Right: Get In Touch form (static, no backend yet) -->
    <div class="contact-form">
      <h2>Get In Touch</h2>
      <p>Share your requirement and we’ll get back to you.</p>

      <form action="#" method="post">
        <div class="field-row">
          <div class="field">
            <label for="name">Name</label>
            <input id="name" name="name" type="text" placeholder="Your full name" required />
          </div>
          <div class="field">
            <label for="email">Email</label>
            <input id="email" name="email" type="email" placeholder="you@example.com" required />
          </div>
        </div>

        <div class="field-row">
          <div class="field">
            <label for="phone">Phone</label>
            <input id="phone" name="phone" type="tel" placeholder="+91 XXXXX XXXXX" />
          </div>
          <div class="field">
            <label for="type">Enquiry Type</label>
            <select id="type" name="type">
              <option>Sales Enquiry</option>
              <option>Service Enquiry</option>
              <option>General Query</option>
            </select>
          </div>
        </div>

        <div class="field">
          <label for="message">Message</label>
          <textarea
            id="message"
            name="message"
            rows="4"
            placeholder="Tell us briefly about your application, machine or plant..."
          ></textarea>
        </div>

        <button type="submit" class="submit-btn">Send Message</button>
      </form>
    </div>
  </section>
</BaseLayout>

<style>
  .page-header {
    margin-bottom: 2rem;
  }

  .page-header h1 {
    font-size: 2.2rem;
    margin-bottom: 0.25rem;
  }

  .page-header p {
    color: #6b7280;
    margin: 0;
  }

  .contact-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.1fr) minmax(0, 1.4fr);
    gap: 2rem;
    align-items: flex-start;
  }

  .contact-info,
  .contact-form {
    background: #ffffff;
    border-radius: 16px;
    padding: 1.75rem;
    box-shadow: 0 4px 18px rgba(15, 23, 42, 0.06);
  }

  .contact-info h2,
  .contact-form h2 {
    margin-top: 0;
    margin-bottom: 0.75rem;
  }

  .label {
    font-weight: 600;
    margin-bottom: 0.25rem;
    margin-top: 1rem;
  }

  .phone a {
    text-decoration: none;
    color: #0b2b4c;
    font-weight: 600;
  }

  .call-note {
    margin-top: 1.25rem;
    font-size: 1.05rem;
    color: #0b2b4c;
  }

  .call-btn {
    display: inline-block;
    margin-top: 0.75rem;
    padding: 0.7rem 1.5rem;
    border-radius: 999px;
    background: #0b2b4c;
    color: #ffffff;
    text-decoration: none;
    font-weight: 600;
  }

  .contact-form p {
    margin-top: 0;
    margin-bottom: 1.25rem;
    color: #4b5563;
  }

  form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .field-row {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
  }

  .field label {
    display: block;
    font-size: 0.9rem;
    margin-bottom: 0.35rem;
    font-weight: 600;
  }

  .field input,
  .field select,
  .field textarea {
    width: 100%;
    border-radius: 10px;
    border: 1px solid #d1d5db;
    padding: 0.55rem 0.75rem;
    font: inherit;
    background: #f9fafb;
  }

  .field textarea {
    resize: vertical;
  }

  .submit-btn {
    margin-top: 0.5rem;
    padding: 0.8rem 1.6rem;
    border-radius: 999px;
    border: none;
    background: #0b2b4c;
    color: #ffffff;
    font-weight: 600;
    cursor: pointer;
  }

  .submit-btn:hover {
    opacity: 0.95;
  }

  @media (max-width: 900px) {
    .contact-grid {
      grid-template-columns: 1fr;
    }

    .field-row {
      grid-template-columns: 1fr;
    }
  }
</style>
